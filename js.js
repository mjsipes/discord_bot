function name(params) {

    
}
//write a function that takes in a string and returns the string with all capital letters
//example: shout("hello") --> "HELLO"

function shout(string) {
    return string.toUpperCase(); // Fixed typo in method name
}


//use the shout function
shout("hello");
